# Core dependencies for EEG-to-Text HMM Model Package

# Deep Learning
torch>=1.9.0

# Numerical computing
numpy>=1.19.0

# Data handling
pandas>=1.2.0

# Note: If you only need inference (not training), you can use:
# torch>=1.9.0 --index-url https://download.pytorch.org/whl/cpu
# This installs a smaller CPU-only version of PyTorch
